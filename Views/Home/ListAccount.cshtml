@model IEnumerable<DOAN_WEB.Models.KantanjiModel.USER>

@{
    ViewBag.Title = "ListAccount";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>ListAccount</h2>

<p>
    @Html.ActionLink("Create New", "CreateAccount","Home")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TAIKHOAN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ENPASSWORD)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TYPE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TAIKHOAN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ENPASSWORD)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TYPE)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDUSER }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDUSER }) |
            @Html.ActionLink("Delete", "Delete",  new { id=item.IDUSER })
        </td>
    </tr>
}

</table>
